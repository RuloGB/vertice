# Tasks: Domain Model and Type Contract

> Trace: T2 (Phase 0) / enables CA-2, CA-3, CA-4, CA-5, CA-13; extends CA-17 gate surface.

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~500-700 hand-written Rust (7 model files + `identity.rs` logic + `tests/model_contract.rs`) + ~100-150 generated `frontend/src/bindings/*.ts` (committed) + ~30 CI/config lines |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 (types + identity + contract tests, no `ts-rs` export) → PR 2 (`ts-rs` wiring, generated bindings, CI gate, license/MSRV verification) |
| Delivery strategy | ask-on-risk → resolved to **single-pr** |
| Chain strategy | n/a — not chained |

Decision needed before apply: **Resolved** (2026-08-17)
Chained PRs recommended: Yes (recommendation **declined**)
400-line budget risk: High

### Size Exception — Recorded

**`size:exception` accepted by the user on 2026-08-17.** T2 ships as a **single PR**, not the chained split suggested above.

The recommendation to split was made and declined; this is a deliberate call, not an oversight. The reviewer receives roughly 650 changed lines in one pass, mixing domain modeling with generation plumbing.

To keep the exception honest, apply MUST:

1. Structure commits as reviewable work units in the phase order below, so the PR reads as a sequence even though it merges as one — the domain modeling and identity logic must be separable from the `ts-rs`/CI plumbing in the commit history.
2. Call out in the PR description that `frontend/src/bindings/*.ts` (~100-150 lines) is machine-generated and should be skimmed, not line-reviewed, and that it is regenerated by `cargo test -p vertice-core`.
3. Label the PR `size:exception`.

The suggested work-unit table below is retained as the **commit** boundary guide, no longer as a PR boundary.

### Suggested Work Units

| Unit | Goal | Likely PR | Notes |
|------|------|-----------|-------|
| 1 | 9 types + `ScanError`, `Serialize`/`Deserialize` derives, `ComponentId::derive`, `tests/model_contract.rs` | PR 1 | No `#[ts(export)]` yet; compiles and tests pass standalone |
| 2 | `TS` derives + `#[ts(export)]`, generated `frontend/src/bindings/*.ts`, ESLint ignore, CI in-sync gate, license/MSRV verification | PR 2 (base: PR 1 branch or main) | Note: generated bindings inflate this PR's diff even though machine-generated |

## Phase 1: Dependency Wiring (read-only checks first)

- [x] 1.1 Add `ts-rs = "12"`, `unicode-normalization = "0.1"` to root `Cargo.toml` `[workspace.dependencies]`.
- [x] 1.2 Add both as regular deps (default features ON) in `crates/vertice-core/Cargo.toml`.
- [x] 1.3 **Verification (read-only):** run `RUSTUP_TOOLCHAIN=1.88 cargo check --workspace --locked --all-targets` (design §4.3). `--all-targets` is load-bearing (ts-rs export fns are `#[test]`s). If it fails, bump MSRV in `Cargo.toml`, `ci.yml` `MSRV` env, and `msrv` job name in lockstep. **Result: PASSED on the first try with `rust-version = 1.88`, no bump needed.** Re-verified after all model/tests/binding code landed — still passes.
- [x] 1.4 **Verification (read-only):** run `cargo deny check licenses` locally, record the delta (design §4.2), BEFORE editing `ci.yml`. Output feeds Phase 6.3's decision. **Result: `licenses ok` — zero delta against the pre-existing allow-list. No new entries needed for `ts-rs`, `ts-rs-macros`, or `unicode-normalization`.**

## Phase 2: Core Types (Foundation, zero I/O)

- [x] 2.1 Create `crates/vertice-core/src/model/mod.rs`: purity/no-I/O module doc + `pub use` re-exports.
- [x] 2.2 Create `component.rs`: `Component`, `ComponentKind` (closed, no `#[non_exhaustive]`), `Scope` (closed) — `#[serde(rename_all = "camelCase")]` (spec: Scope Is Always Populated, ComponentKind Is Closed).
- [x] 2.3 Create `location.rs`: `Location`, `LocationOrigin`, `SearchRoot`, `SearchRootId`, `SearchRootKind` — `path: Option<PathBuf>` (spec: Location Path Is Optional).
- [x] 2.4 Create `installation.rs`: `ClientInstallation`, `ClientKind`.
- [x] 2.5 Create `report.rs`: `ScanReport`, `ScanIssue`, `IssueSeverity` (exactly `Warning`/`Error`, neither aborts) (spec: ScanIssue Severity, Empty Scan Result).
- [x] 2.6 Create `error.rs`: `ScanError` (`thiserror`, adjacent-tagged `Serialize`, no `Deserialize`; every variant payload owned/serializable, never a foreign error type) (design §7).
- [x] 2.7 Wire `pub mod model;` in `crates/vertice-core/src/lib.rs`.

## Phase 3: Identity Derivation — TDD

- [x] 3.1 [RED] Write `identity.rs` `#[cfg(test)]`: case-variant collapse, same-pair-equal, different-kind-differs, delimiter safety (spec: Component Identity Is Deterministic). Also added an NFC/NFD equivalence test (triangulation) per design §3's explicit macOS/Linux drift concern.
- [x] 3.2 [GREEN] Implement `normalize_name` (trim → NFC via `unicode-normalization` → `to_lowercase()`) and `ComponentId::derive(kind, name)` = `"{kind}:{normalized_name}"` (design §3).
- [x] 3.3 [REFACTOR] Confirm `ComponentId(String)` newtype has no dead code; `normalize_name` stays `pub(crate)`. Confirmed — no clippy warnings, no dead-code lints.

## Phase 4: Integration Contract Tests — TDD

- [x] 4.1 [RED] Create `crates/vertice-core/tests/model_contract.rs`: pathless-vs-`Some` distinguishable (CA-1), one `Component`/two `Location`s same id (CA-2), `scope` populated (CA-3), empty `ScanReport` is `Ok`, `Location.root` referential integrity, `serde_json` round-trip. Added `serde_json` as a `[dev-dependencies]` entry (not part of the model's own import allow-list).
- [x] 4.2 [GREEN] Run `cargo test -p vertice-core --locked`; confirm all Phase 2/3 types satisfy the contract tests. **6/6 contract tests pass**, plus a bonus populated-report round-trip test beyond the minimum list.

## Phase 5: `ts-rs` Bindings

- [x] 5.1 Add `TS` derive + `#[ts(export, export_to = "...")]` to all 8 types (plus their nested enums/newtypes, per spec: "and their nested enums MUST derive...") and `ScanError`.
- [x] 5.2 Run `cargo test -p vertice-core --locked` to generate `frontend/src/bindings/*.ts`. **See "Deviation: `export_to` path depth" below — the design-specified `"../../frontend/src/bindings/"` was empirically wrong by one level; corrected to `"../../../frontend/src/bindings/"` after reproducible verification.**
- [x] 5.3 **Verification:** inspect generated `Location.ts` for the emitted `Option<PathBuf>` shape (design §4.1). Expect `path: string | null`. **Result: confirmed exactly `path: string | null`, no attribute fallback needed.**
- [x] 5.4 Commit generated `frontend/src/bindings/*.ts`. **Not committed per this session's git discipline (no commits are made by apply) — left staged-ready in the working tree for the user to review and commit. See "Intended Commit Boundaries" in `apply-progress.md`.**

## Phase 6: CI & Tooling Wiring

- [x] 6.1 Add `src/bindings/` to `frontend/eslint.config.*` ignore list; keep it inside `tsconfig` `include` (design §5). `tsconfig.json`'s existing `"src/**/*.ts"` include already covered it — no tsconfig change needed.
- [x] 6.2 Add "Regenerate TS bindings" (`cargo test -p vertice-core --locked`) + "Generated contract in sync" (`git diff --exit-code -- frontend/src/bindings`) steps to the `quality` job in `.github/workflows/ci.yml`, after `cargo deny`.
- [x] 6.3 Per Phase 1.4's delta (zero — clean): extended `quality` to `cargo deny check bans licenses` (single step, both checks). Documented the rationale in `deny.toml`'s header comment alongside T1's existing `advisories` deferral rationale.

## Phase 7: Manual and Full-Suite Verification (record in verify-report.md, not automated)

- [x] 7.1 **MANUAL, one-time:** mutate a model field name, run `cargo test -p vertice-core --locked` WITHOUT committing, confirm `git diff --exit-code -- frontend/src/bindings` exits non-zero (CA-4 negative path), then revert (design §11). **Performed exactly as specified: `ClientInstallation.version` → `client_version` (2-file edit), regenerated, `git diff --exit-code` returned exit code 1 with a minimal single-field diff on `ClientInstallation.ts`, then fully reverted (both Rust files and the regenerated binding) and re-confirmed exit code 0. `git status` matches the pre-mutation baseline.**
- [x] 7.2 Confirm `cargo test -p vertice-core --locked`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo deny check bans` (+ `licenses` if enabled) all pass. **All pass — see apply-progress.md for full command output summary.**
- [x] 7.3 Confirm `npm run lint` and `npm run check` pass with `frontend/src/bindings/` present. **Both pass — `npm run lint`: clean; `npm run check`: 168 files, 0 errors, 0 warnings.**
