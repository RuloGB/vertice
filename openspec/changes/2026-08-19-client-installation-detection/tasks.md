# Tasks: Client Installation Detection

> Trace: **T7** (`internal-docs/plan-desarrollo-poc.md:171-187`) / closes **CA-7** (two Claude Code installs never merged) and **CA-11** (absent client reported as an explicit not-detected signal, never a silent empty list); bound by **CA-16** (read-only) and **CA-17** (fixture-based, new non-reused tree, three-platform CI). Design: `openspec/changes/2026-08-19-client-installation-detection/design.md`. Spec: `openspec/changes/2026-08-19-client-installation-detection/specs/client-installation-detector/spec.md`. Inherits T4/T5/T6's fixture-per-case pattern and PR-boundary precedent.
> Core-only — no IPC, no Tauri command, no frontend source change. `crates/vertice-core/src/model/`, `frontend/src/bindings/`, `crates/vertice-core/src/roots.rs`, `Cargo.toml`/`Cargo.lock`/`deny.toml` are **all unchanged** by design (§1, §2, §3, §5.3). **If any task below appears to require editing one of these four, STOP and flag it — the design closed all four and no task in this list should need to reopen them.**
> `strict_tdd: true`. Fixtures and RED tests land before the implementation that turns them green. Design §10 marks `two-claude/` (CA-7), `nothing/` (CA-11), `isolation/`, and `npm-dir-no-package-json/` **NON-NEGOTIABLE** — each gets its own checkbox below, not a bullet folded into a generic fixture task.
> Environment note: `cargo` may not be on PATH in this environment. Do not claim gates pass; report their real status (pass / fail / not-run) honestly.

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~445–685 (design §12) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 → PR 2 (design §12's own seam) |
| Delivery strategy | ask-on-risk |
| Chain strategy | n/a — **single PR with `size:exception`, chosen by the user 2026-08-19** |

Decision needed before apply: **RESOLVED 2026-08-19** — the user chose a **single PR with `size:exception`**, declining the chained split.
Chained PRs recommended: Yes (recommendation not taken; recorded, not overridden silently)
Chain strategy: n/a — single PR
400-line budget risk: High — accepted under `size:exception`, following the T6 precedent

The two work units below therefore become **commit-group boundaries inside one PR**, not separate PRs. The phase ordering and the strict-TDD RED-before-GREEN sequencing are unaffected.

### Suggested Work Units

| Unit | Goal | Likely PR | Notes |
|------|------|-----------|-------|
| 0 | U1 path re-verification evidence (no code; folds into PR 1's body) | — | Blocking gate before Phase 1 fixtures are shaped, see Phase 0 |
| 1 | `installations.rs` (probe table, `HostPlatform`, `InstallationScan`, stub `scan_for`), `lib.rs` wiring, full `tests/fixtures/installations/**` tree (12 homes) incl. `.gitattributes` binary line and `.gitkeep` tripwire, probe-table + platform-seam unit tests | PR 1 (~200–300 lines) | Base `main`. Compiles and is green on merge; `two-claude`/CA-7 integration test lands RED as PR 2's first commit. |
| 2 | `resolve`, both version sources, the §8 issue taxonomy, `tests/client_installations.rs` full RED→GREEN integration suite | PR 2 (~245–385 lines) | Base = PR 1's branch (feature-branch-chain) or `main` (stacked) — **depends on the chain strategy the user has not yet picked**. RED-before-GREEN preserved by commit order; `two-claude` failing first is non-negotiable (tasks 2.3–2.4). |

**Both slices individually risk exceeding 400 lines on their own upper bound** (PR 1's ~300 is safely under; PR 2's ~385 is close but plausible under, ~445–685 total is the risk). This is a genuine two-way fork with a real tradeoff, not a formality — see the "chained PRs vs `size:exception`" precedent T6 set (single PR accepted by explicit user decision) versus T5's genuine two-PR split. **The orchestrator/user must pick a chain strategy (stacked-to-main, feature-branch-chain, or size:exception) before `sdd-apply` starts Phase 1** — this task list does not assume one. If `size:exception` is chosen, the two work units above collapse into commit-group boundaries inside one PR, mirroring T6's precedent; the phase ordering below is unaffected either way.

## Phase 0: U1 — Re-Confirm The Three Windows Probe Paths (BLOCKING — before Phase 1 fixtures)

Design §13/§0 U1: the three paths were empirically checked against a real machine in August 2026 (`alcance-poc-vertice.md:71-102`) but never re-verified from the design phase, which had no shell. A stale or mistyped path yields a fully green fixture suite and a false "not detected" on real hardware.

- [x] 0.1 **DONE — verified 2026-08-19 by direct inspection of the reference machine.** All three Windows probe paths were confirmed to exist: `AppData\Roaming\npm\node_modules\@anthropic-ai\claude-code`, `AppData\Roaming\Claude\claude-code\<version>` (with **two** versioned subdirectories present, `2.1.229` and `2.1.234`), `AppData\Roaming\npm\node_modules\opencode-ai`. `package.json` `"version"` values: `AppData\Roaming\npm\node_modules\@anthropic-ai\claude-code\package.json` → `2.1.140`; `AppData\Roaming\npm\node_modules\opencode-ai\package.json` → `1.17.20`. These match `alcance-poc-vertice.md:71-102`'s recorded values.
- [x] 0.2 **DONE — outcome recorded: "paths confirmed unchanged, U1 verified; U2 refuted."** U1 (the three paths) is confirmed current. U2 (the closed decision that exactly one desktop version directory ever exists) is **refuted**: `2.1.229` and `2.1.234` coexist under `AppData\Roaming\Claude\claude-code\` on the reference machine. Design §0 and §6 were corrected accordingly (see `design.md`).
- [x] 0.3 **Checkpoint satisfied.** 0.1–0.2 are recorded with a positive verification outcome; Phase 1 fixture creation may proceed, with `desktop-two-versions/` and `reference/` shaped to the corrected N-installations rule (design §6, §10).

## Phase 1: Probe Table, Platform Seam, Fixture Tree, RED Tests (PR 1)

- [x] 1.1 [RED] In `crates/vertice-core/src/installations.rs` (new), add `#[cfg(test)]` unit tests for `windows_install_probes(home: &Path) -> [InstallProbe; 3]`: exactly 3 entries in the fixed order Claude Code npm → Claude Code desktop → OpenCode npm (design §11); each `path` is `home` plus the hardcoded segments from §11's table, pushed one at a time (never `dirs`/`directories`, never an env read); `VersionSource` is `PackageJson` for both npm slots and `DirectoryName` for the desktop slot; identical structure for two different `home` values. No disk access. — *spec: "Windows Probe Paths Are Hardcoded, Never OS-Convention-Derived"*
- [x] 1.2 [GREEN] Implement the private types `InstallProbe { client: ClientKind, kind: InstallKind, path: PathBuf, version: VersionSource }`, `enum InstallKind { Npm, Desktop }`, `enum VersionSource { PackageJson, DirectoryName }`, and `fn windows_install_probes(home: &Path) -> [InstallProbe; 3]` per design §5.1/§11. Pass 1.1.
- [x] 1.3 [RED] Add unit tests for `HostPlatform`: `HostPlatform::current()` uses `cfg!(target_os = "windows")` as an **expression**, never `#[cfg(target_os = ...)]` as an attribute (design §5.2 — the attribute form would compile `windows_install_probes` out entirely on non-Windows CI legs and break the build, not just the coverage); both variants (`Windows`, `Unsupported`) are constructible and inspectable on every target.
- [x] 1.4 [GREEN] Implement `pub enum HostPlatform { Windows, Unsupported }` (no `Serialize`, no `TS`), `pub struct InstallationScan { pub installations: Vec<ClientInstallation>, pub issues: Vec<ScanIssue> }` (no `roots` field — design §3), and `pub fn scan_for(home: &Path, platform: HostPlatform) -> InstallationScan` that builds the platform's probe table but returns an **empty scan** (`resolve` is Phase 2's job — design §12's PR-1/PR-2 seam). Add `pub fn scan(home: &Path) -> InstallationScan` delegating to `scan_for(home, HostPlatform::current())`. Pass 1.3. **[Deviation: implemented alongside 2.5's `resolve` in one pass rather than a separate empty-scan stub, per the `size:exception` single-PR collapse — see apply-progress.md.]**
- [x] 1.5 Wire `pub mod installations;` in `crates/vertice-core/src/lib.rs` — one line, no crate-root re-export, matching `lib.rs:7-12`'s existing style.
- [x] 1.6 Create the fixture group directory `crates/vertice-core/tests/fixtures/installations/` (design §10) — a new tree, never reused from any T4/T5/T6 fixture tree (CA-17). Every home is a synthetic `%USERPROFILE%` containing `AppData/Roaming/...`, paths built via `env!("CARGO_MANIFEST_DIR")` + per-segment `push`, never `"/"`-joined literals (`tests/skill_scanner.rs:23-30`).
- [x] 1.7 **[NON-NEGOTIABLE — CA-11 PIN]** Create `nothing/` — `.gitkeep` only, no `AppData` tree at all → expected: 0 installations, exactly 3 `Warning` issues, each carrying its own probe path.
- [x] 1.8 **[NON-NEGOTIABLE — CA-7 PIN]** Create `two-claude/` — npm `@anthropic-ai/claude-code/package.json` declaring `"version": "1.0.100"` + `Claude/claude-code/2.5.3/` → expected: exactly 2 `ClientInstallation` values, both `client: ClaudeCode`, **different** versions (`1.0.100` and `2.5.3`), distinct paths, 0 issues. The two versions MUST differ — a fixture with identical versions would pass under a merging bug and prove nothing (design §10). **[Deviation: also carries a healthy OpenCode npm install, to satisfy the fixture's own literal "0 issues" pin — see apply-progress.md.]**
- [x] 1.9 **[NON-NEGOTIABLE]** Create `isolation/` — malformed `claude-code/package.json` + a healthy `opencode-ai/package.json` + a healthy desktop dir → expected: exactly 1 `Error` on the malformed slot, and the other two installations still detected and reported.
- [x] 1.10 **[NON-NEGOTIABLE]** Create `npm-dir-no-package-json/` — the npm package directory present, no `package.json` inside it → expected: 1 `Error`, and **zero** `Warning` (a broken install must never read as "not detected" — this is CA-11's counterpart guarantee).
- [x] 1.11 Create the remaining eight fixture homes: `opencode-npm/` (`opencode-ai/package.json` `"0.4.2"` → 1 `OpenCode` installation); `no-version-key/` (valid JSON, no `"version"` key → 0 for that slot, 1 `Error`); `version-not-a-string/` (`"version": 3` → 0 for that slot, 1 `Error`); `package-json-empty/` (zero-byte `package.json` → same collapsed `Error` reason as `no-version-key/`, per V5's empty-input edge); `package-json-unreadable/` (non-UTF-8 bytes → 1 `Error`, `could not read package.json: {io}`, distinct from `isolation/`'s parse failure — this one fails at `read_to_string`, before `jsonc::parse` runs); `desktop-empty/` (`Claude/claude-code/.gitkeep`, no versioned subdirectory → 0, 1 `Error`); `desktop-two-versions/` (`Claude/claude-code/{1.0.0,2.0.0}/` → **2 installations, both `ClaudeCode` desktop, DIFFERENT versions, distinct paths, 0 issues** — never merged, never an anomaly, CA-7 pin, design §6/§10); `reference/` (npm Claude Code + **two** desktop Claude Code versions + npm OpenCode, realistic `package.json` shapes, mirroring the verified reference machine → **4 installations**, 0 issues). **[Deviation: `desktop-two-versions/` also carries healthy npm Claude Code + OpenCode installs to satisfy its own literal "0 issues" pin — see apply-progress.md.]**
- [x] 1.12 Add a dedicated `.gitattributes` line marking `crates/vertice-core/tests/fixtures/installations/package-json-unreadable/AppData/Roaming/npm/node_modules/@anthropic-ai/claude-code/package.json` as `binary`, mirroring `.gitattributes:5`'s precedent for `frontmatter/non-utf8-content/SKILL.md` — the blanket `-text` scope on `.gitattributes:2` does not fully suppress diff/merge attempts on non-UTF-8 content; a missed line risks Git's text normalization silently corrupting the fixture.
- [x] 1.13 Write the `.gitkeep` tripwire disk-existence test: assert via `std::fs::metadata` that `desktop-empty/AppData/Roaming/Claude/claude-code/` exists on disk before any scanner code runs (T4D §8 precedent). Losing this `.gitkeep` would silently turn "empty desktop directory" into "desktop absent" (`Error` → `Warning`) — this test catches the loss directly rather than relying on the severity mismatch to surface it later.
- [x] 1.14 **[Gate, end of PR 1]** `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace --locked` (all green, including 1.1/1.3's unit tests and 1.13's tripwire); `cargo deny check bans licenses` (green — no dependency change). Confirm `git diff --exit-code -- frontend/src/bindings` and `git diff --exit-code -- crates/vertice-core/src/model` are both clean — PR 1 introduces zero model or binding changes. Report results honestly; if `cargo` is unavailable in this environment, report as not-run, never assumed passing. **All gates actually run and green — see apply-progress.md for full output.**

## Phase 2: `resolve`, Version Extraction, `ScanIssue` Assembly — TDD (PR 2)

**Read before writing code in this phase.** Design §10 is explicit: the `two-claude/` fixture "must exist and FAIL before the assembly code is written." Task 2.3's RED assertion must be observed failing before 2.5's `resolve` implementation begins — this is the load-bearing safeguard for CA-7, mirroring T6's `partial-override` checkpoint discipline.

- [x] 2.1 [RED] In `installations.rs`, add `#[cfg(test)]` unit tests for value-level version extraction on `jsonc::JsonValue` literals (no disk, no regex, no `#[derive(Deserialize)]` DTO — design §5.4): `"version"` present as `JsonValue::String` → `Some(String)`; `"version"` absent, non-string, empty string, or an empty document → `None` in every case (the collapsed row, design §8/V5). — *spec: "Version Is Extracted From The Correct Source Per Slot" + "A Malformed Or Unreadable package.json Produces An Error"*
- [x] 2.2 [GREEN] Implement the private `PackageJson`-source extraction helper: `entry.get("version")` matched against `JsonValue::String` only. Pass 2.1.
- [x] 2.3 [RED] Create `crates/vertice-core/tests/client_installations.rs` and write **first**, before any other test in this file, the `two-claude/` fixture assertion (task 1.8's fixture): the scan over `two-claude/` MUST produce exactly 2 `ClientInstallation` values, both `client: ClaudeCode`, with the two distinct versions and distinct paths, and 0 issues. This currently fails — either to compile (PR 1's `resolve` does not exist) or on assertion (PR 1's `scan_for` stub returns an empty scan) — that is expected and is this change's primary CA-7 safeguard.
- [x] 2.4 **[Checkpoint — RUN AND OBSERVED FAILING.** `resolve` was temporarily stubbed out to an empty no-op inside `scan_for` and `cargo test -p vertice-core --locked --test client_installations` was run: `two_claude_fixture_yields_two_never_merged_claude_installations` FAILED with `assertion left == right failed: exactly two ClaudeCode installations, never merged / left: 0 / right: 2`. The stub was then reverted and the real `resolve` call restored before 2.5 began. Full transcript in apply-progress.md.**
- [x] 2.5 [GREEN] Implement `resolve` per design §5.1/§5.3/§6/§8, noting design §6's correction: the desktop slot resolves to **N `ClientInstallation` values, one per versioned subdirectory**, so its resolution path is no longer the single-`Option` shape used for the two npm slots. npm slots via a private `fn exists(path: &Path) -> bool` over `std::fs::symlink_metadata` (`NotFound` ⇒ `false`, else `true` — never `roots::probe`, which stays private and untouched per design §5.3) + `read_to_string` + `jsonc::parse` + 2.2's extraction. Desktop slot via `read_dir`, candidates collected and **sorted by file name byte-wise** (never locale collation — design §7) before use; 0 candidates → no installation, 1 `Error` (design §6); N ≥ 1 candidates → N `ClientInstallation` values in sorted order, never merged, never an anomaly; non-UTF-8 candidate names handled per-candidate, mirroring `skills.rs:118-125`, without blocking any other valid candidate from resolving. Wire the resolution into `scan_for`, replacing PR 1's empty-scan stub. Pass 2.3.
- [x] 2.6 Wire the complete `ScanIssue` taxonomy per design §8's table: absent npm/desktop dir → `Warning`, `{Client} ({kind}) not detected`; npm dir present but `package.json` absent, unreadable, unparseable, non-object root, missing/non-string/empty `"version"` → `Error` (the three version-shape cases collapsed to one reason, per design §8); desktop 0 candidates → `Error` per §6's exact reason; desktop candidate with a non-UTF-8 name → `Error` per §6's exact reason (that candidate alone, never blocking any other valid candidate); desktop N ≥ 1 valid candidates → **no `ScanIssue`, N `ClientInstallation` values** (§6, never an anomaly regardless of N); desktop `read_dir` failing for any other reason → `Error`; `HostPlatform::Unsupported` → `Warning`, `path: None`, `"client installation detection is not implemented on this platform"`. No `escalate` function — every issue is constructed where caller context is already in hand (T6D §5.6 precedent).
- [x] 2.7 [RED] Add the remaining integration tests to `tests/client_installations.rs`, one per fixture from Phase 1, each pointed at its own home:
  - `nothing/` — 0 installations, exactly 3 `Warning` issues, each with its probe path, **no `Error`** (task 1.7, CA-11)
  - `opencode-npm/` — 1 installation, `client: OpenCode`
  - `isolation/` — 1 `Error` on the malformed slot, the other two installations still present (task 1.9)
  - `no-version-key/` and `version-not-a-string/` — 0 for that slot, 1 `Error` each
  - `package-json-empty/` — 0 for that slot, 1 `Error`, same reason string as `no-version-key/`
  - `package-json-unreadable/` — 1 `Error` (`could not read package.json: {io}`), zero `Warning`, distinct from `isolation/`'s parse-failure reason
  - `npm-dir-no-package-json/` — 1 `Error`, zero `Warning` (task 1.10)
  - `desktop-empty/` — 0 installations, 1 `Error`, never a phantom entry
  - `desktop-two-versions/` — exactly 2 `ClientInstallation`, both `ClaudeCode` desktop, DIFFERENT versions, distinct paths, **0 issues** — never merged, never an anomaly, never "highest wins" (CA-7 pin, task 1.11)
  - `reference/` — exactly 4 installations (npm Claude Code, two desktop Claude Code versions, npm OpenCode), 0 issues
  - platform seam: `scan_for(home, HostPlatform::Unsupported)` → 0 installations, exactly 1 `Warning`, `path: None`
  - entry-point dispatch: `scan(home)` on `reference/` matches `scan_for(home, Windows)` on the Windows CI leg and `scan_for(home, Unsupported)` on the Linux/macOS legs, asserted via `cfg!(target_os = "windows")`
  - determinism: two consecutive scans over `reference/` and over `desktop-two-versions/` yield byte-identical installation and issue vectors
  - contract: no `ClientInstallation` ever carries an empty `version`
  - read-only (CA-16): a full scan over `reference/` leaves the fixture tree byte-for-byte unchanged (`fixture_tree_bytes` before/after, `tests/skill_scanner.rs:234-258` pattern)
- [x] 2.8 [GREEN] Confirm all of 2.7 passes with no logic changes beyond 2.5/2.6 — this task exists to make the full spec-to-fixture mapping a checked fact rather than an assumption.
- [x] 2.9 [REFACTOR] Confirm `InstallProbe`, `InstallKind`, `VersionSource`, `resolve`, `windows_install_probes`, and `exists` stay private; only `InstallationScan`, `HostPlatform`, `scan`, `scan_for` are `pub` from `installations.rs`. `cargo clippy --workspace --all-targets -- -D warnings` clean.

## Phase 3: Verification (local, pre-commit gates)

- [x] 3.1 `cargo fmt --all --check`.
- [x] 3.2 `cargo clippy --workspace --all-targets -- -D warnings`.
- [x] 3.3 `cargo test --workspace --locked` — Phase 1 and Phase 2 suites, in-module units, all green, including T4/T5/T6's existing suites and the `roots.rs` unit suite staying green with **zero edits**.
- [x] 3.4 `cargo deny check bans licenses` — expected unaffected; no new dependency (design §1: `jsonc.rs` reused, no second JSON crate).
- [x] 3.5 **Read-only grep (CA-16)**: confirm no `File::create`, `OpenOptions::write`, `fs::write`, `create_dir*`, or `remove_*` anywhere in `installations.rs` or `tests/client_installations.rs`. The complete disk surface is `std::fs::symlink_metadata`, `std::fs::read_to_string`, `std::fs::read_dir` (design §1).
- [x] 3.6 **Domain-model and bindings invariant**: confirm `git diff --exit-code -- crates/vertice-core/src/model` and `git diff --exit-code -- frontend/src/bindings` are **both clean** — zero lines changed in either (design §2). Run the diff and record its actual output.
- [x] 3.7 **Seam invariants**: grep confirms no `dirs`/`directories` import, no `std::env` read, no `regex`/`Regex` import, and no second JSON-parsing crate anywhere in `installations.rs` — `jsonc.rs` is reused unmodified as the sole JSON/JSONC seam.
- [x] 3.8 **`roots.rs` invariant**: confirm `git diff --exit-code -- crates/vertice-core/src/roots.rs` is clean — zero lines changed, not even a visibility change (design §5.3).
- [x] 3.9 **Dependency invariant**: confirm `Cargo.toml`, `Cargo.lock`, and `deny.toml` are byte-identical to their pre-change state — no new dependency introduced (design §1).
- [x] 3.10 **Platform note**: all three CI legs (`ubuntu-24.04`, `windows-2022`, `macos-14`) exercise the Windows probe table via `scan_for(home, HostPlatform::Windows)` regardless of host OS (design §5.2's payoff — a synthetic `home` containing `AppData/Roaming/...` is just directory names to any filesystem). Windows is the only platform whose real paths were empirically checked (Phase 0); macOS/Linux path tables are **T16**, and `HostPlatform::Unsupported` is the honest placeholder until then. Only the Windows leg was actually run in this apply session (this environment); Linux/macOS legs are unverified until CI runs.
- [x] 3.11 From `frontend/`: `npm run lint && npm run check && npm run test && npm run build` — regression check only; this change adds no consumer of any binding and no new binding exists to consume.
